import { Box, styled } from '@mui/material';

export const FilterItem = styled(Box)({
  marginBottom: '30px',
})

export const HeaderTow = styled('h2')({
  fontWeight: 400,
  marginBottom: '20px',

})

export const InputItem = styled(Box)({
  marginBottom: '10px',

})

export const InputLabel = styled('label')({
  marginLeft: '10px',
})